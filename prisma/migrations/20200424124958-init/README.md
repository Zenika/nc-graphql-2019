# Migration `20200424124958-init`

This migration has been generated by Nicolas Leport at 4/24/2020, 12:49:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Plaine" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Mare" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" text  NOT NULL ,
    "nom" text  NOT NULL ,
    "plaineId" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Canard" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" text  NOT NULL ,
    "isAffame" boolean  NOT NULL DEFAULT false,
    "mareId" text  NOT NULL ,
    "nom" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Poisson" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" text  NOT NULL ,
    "isCanarovore" boolean  NOT NULL DEFAULT false,
    "isGros" boolean  NOT NULL DEFAULT false,
    "mareId" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

ALTER TABLE "public"."Mare" ADD FOREIGN KEY ("plaineId")REFERENCES "public"."Plaine"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Canard" ADD FOREIGN KEY ("mareId")REFERENCES "public"."Mare"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Poisson" ADD FOREIGN KEY ("mareId")REFERENCES "public"."Mare"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200424124958-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,45 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Plaine {
+  id          String    @id @default(uuid())
+  createdAt   DateTime  @default(now())
+  mares       Mare[]
+}
+
+model Mare {
+  id          String    @id @default(uuid())
+  createdAt   DateTime  @default(now())
+  nom         String
+  plaine      Plaine    @relation(fields: [plaineId], references: [id])
+  plaineId    String
+  canards     Canard[]
+  poissons    Poisson[]
+}
+
+model Canard {
+  id          String    @id @default(uuid())
+  createdAt   DateTime  @default(now())
+  mare        Mare      @relation(fields: [mareId], references: [id])
+  mareId      String
+  nom         String
+  isAffame    Boolean   @default(value: false)
+}
+
+model Poisson {
+  id            String    @id @default(uuid())
+  createdAt     DateTime  @default(now())
+  mare          Mare      @relation(fields: [mareId], references: [id])
+  mareId        String
+  isGros        Boolean   @default(value: false)
+  isCanarovore  Boolean   @default(value: false)
+}
```


